#include <stdio.h>

/*
ループ(for)
*/

int main(void)
{
    int m;
    for (m = 0; m < 10; m++)
    {
        printf("m: %d\n", m);
    }

    /*
     int m = 0;
    while (m < 10)
    {
        printf("m: %d\n", m);
        m++;
    }
    */
}